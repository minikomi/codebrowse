[project]
name = "codebrowse"
version = "0.1.0"
description = "Minimal file browser with syntax highlighting"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "pygments",
]

[project.scripts]
codebrowse = "app:main"
